# Base URL of the Indwella FastAPI backend (no trailing slash).
# Android emulator can't reach "localhost" (that's the emulator itself), so
# it uses the special alias 10.0.2.2 to reach your machine's localhost.
API_BASE_URL=http://10.0.2.2:8000
